busqueda on input?
filtro on submit sobre el input

si es comic fetch con comic
si es personaje fetch con personaje
y despues un order -



const orderBy = () => {
  let filterType = checkingFilterType();
  console.log(filterType);
  let orderTitleAZ = "title";
  let orderTitleZA = "-title";
  let orderNameAZ = "name";
  let orderNameZA = "-name";
  let orderTitleOlder = "-focDate";
  let orderTitleNewer = "focDate";

  if (filterType === "comics") {
    if (orderFilter.value === "az") {
      displayingContent(filterType, orderTitleAZ);
    } else if (orderFilter.value === "za") {
      displayingContent(filterType, orderTitleZA);
    } else if (orderFilter.value === "older") {
      displayingContent(filterType, orderTitleOlder);
    } else if (orderFilter.value === "newer") {
      displayingContent(filterType, orderTitleNewer);
    }
  }
  if (filterType === "characters") {
    if (orderFilter.value === "az") {
      displayingContent(filterType, orderNameAZ);
    } else if (orderFilter.value === "za") {
      displayingContent(filterType, orderNameZA);
    }
  }
};
